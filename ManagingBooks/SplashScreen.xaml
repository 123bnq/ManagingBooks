<Window x:Class="ManagingBooks.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagingBooks" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="None"
        mc:Ignorable="d" ShowInTaskbar="False"
        Title="SplashScreen" Height="450" Width="800">
    <StackPanel>
        <StackPanel.Background>
            <ImageBrush ImageSource="/Images/sharon-mccutcheon-532782-unsplash.jpg"/>
        </StackPanel.Background>
        <TextBlock FontSize="50" Text="{DynamicResource MainWindow.Title}" Margin="50,50,50,0"/>
        <TextBlock FontSize="15" Margin="50,0,50,0">
            <Run Text="Version "/>
            <Run Text="{DynamicResource Version}"/>
        </TextBlock>
    </StackPanel>
</Window>
